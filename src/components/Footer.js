import React from "react";

export default function Footer({ children }) {
    return (
        <div>
            <div className="footer-style">
                {children}
            </div>
        </div>
    )
}